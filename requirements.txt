# Core analysis engine
networkx==3.4.2

# Web UI
streamlit>=1.40,<2.0

# Live AWS integration
boto3>=1.35

# Data handling & visualization
# (the attack graph is rendered as a static Graphviz diagram via Streamlit's
#  built-in st.graphviz_chart — no extra graph library needed)
pandas>=2.0
plotly>=5.20

# Encrypted local credential storage
cryptography>=42.0
